# install baudrate in ~/bin

# create ~/bin directory, if it does not exist
[ ! -d ~/bin  ] && mkdir ~/bin

# copy the file to ~/bin, make it executable
cp baudrate ~/bin
chmod +x ~/bin/baudrate

echo "baudrate installed."
echo "Reboot if this is the first time baudrate is installed"
